window.addEventListener('load',()=>setTimeout(()=>document.getElementById('loader').style.opacity='0',250));window.addEventListener('load',()=>setTimeout(()=>document.getElementById('loader').remove(),900));
const menu=document.querySelector('.menu-toggle'),nav=document.querySelector('#nav');menu.addEventListener('click',()=>{nav.classList.toggle('open')});document.querySelectorAll('#nav a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));
const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
const top=document.querySelector('#toTop');window.addEventListener('scroll',()=>top.classList.toggle('show',scrollY>700));top.addEventListener('click',()=>scrollTo({top:0,behavior:'smooth'}));
const modal=document.querySelector('#galleryModal'),modalImg=document.querySelector('.modal-image'),modalTitle=document.querySelector('#modalTitle');document.querySelectorAll('.gallery-item').forEach(item=>item.addEventListener('click',()=>{modal.classList.add('open');modal.setAttribute('aria-hidden','false');modalImg.style.backgroundImage=getComputedStyle(item).backgroundImage;modalTitle.textContent=item.dataset.title}));document.querySelector('.modal-close').addEventListener('click',closeModal);modal.addEventListener('click',e=>{if(e.target===modal)closeModal()});function closeModal(){modal.classList.remove('open');modal.setAttribute('aria-hidden','true')}
const form=document.querySelector('#quoteForm');form.addEventListener('submit',e=>{e.preventDefault();const d=new FormData(form),name=d.get('name');document.querySelector('#formStatus').textContent=`Thanks, ${name}. This demo form works on the page, but you must connect it to a real form/email service before launch.`;form.reset()});
